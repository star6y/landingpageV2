{
    "routes": [
        {
            "route": "/"
        },
        {
            "route": "/aboutme"        
        },
        {
            "route": "/projects"        
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": ["*.{css,scss,js,png,gif,ico,jpg,svg}"]
    }
}
