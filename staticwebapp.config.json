{
    "routes": [
        {
            "route": "/"
        },
        {
            "route": "/aboutme"
        },
        {
            "route": "/projects"
        },
        {
            "route": "/sandbox"
        }
    ],
    "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": ["*.{css,scss,js,png,gif,ico,jpg,svg}"]
    }
}
